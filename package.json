{
  "type": "module",
  "name": "spotify-playlist-filter",
  "version": "1.0.0",
  "description": "This is app gets the user's saved tracks, filter the tracks that are repeated inside the user's saved tracks so the user can decide wether to delete or keep those tracks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
